WidGet file, version 1.1, created:  Wed Jul 30 13:27:06 PDT 1997
Widget scale#1
	configure orient h
	configure to 50.0
	configure tickinterval 10.0
	other error {}
	other tags {}
	other level {}
	other help {}
	configure label Scale
	configure length 25
	other master {}
	other pathname scale#1
	configure borderwidth 4
	other tabbing {}
	other item_name scale#1
	geometry sticky ew
	geometry row 4
	other type scale
	other comments {}
	geometry column 2
	configure relief groove
Widget button#3
	geometry row 2
	other type button
	other error {}
	configure anchor w
	other master {}
	configure text {Add 5 to Scale}
	configure default normal
	other pathname button#3
	other tags {}
	other level {}
	other help {}
	geometry column 2
	other comments {}
	other item_name addButton
	geometry sticky ew
	configure command {set x [%B.scale#1 get]; %B.scale#1 set [expr $x + 5]}
	other tabbing {}
Widget f
	other generic_options {button {
		} canvas {
		} checkbutton {
		} entry {
		} frame {
		} label {
		} listbox {
		} menubutton {
		} message {
		} radiobutton {
		} scale {
		} scrollbar {
		} text {
		}
	}
	other panel {}
	other resize_row {0 0 0 3 0}
	other type frame
	other code {# After you the exAssemS.ui assembly, you should
# run it's partner: exAssemM.ui. The exAssemM.ui
# example loads this subassembly twice to show the
# widget name qualification that enables this to
# function.
}
	other min_row {10 13 30 30 9}
	other master {}
	other pathname f
	other resize_column {0 3 0}
	other tags {}
	other level 0
	configure background #e9e9e9
	other min_column {10 194 10}
	other help {}
	other Colors {black white bisque grey red green blue magenta yellow #AE00B200C300}
	other comments {}
	other item_name f
	other tabbing {}
Widget button#4
	geometry row 3
	other error {}
	other type button
	configure anchor w
	other master {}
	configure text {Sub 5 from Scale}
	configure default normal
	other pathname button#4
	other tags {}
	other level {}
	other help {}
	geometry column 2
	other comments {}
	geometry sticky ew
	other item_name subButton
	other tabbing {}
	configure command {set x [%B.scale#1 get]; %B.scale#1 set [expr $x - 5]}

